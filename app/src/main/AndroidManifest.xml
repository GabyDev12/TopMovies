<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="project.topmovies">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TopMovies">
        <activity android:name=".visual.LoadingScreen_Activity" android:theme="@style/Theme.TopMovies_NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".visual.Start_Activity" android:theme="@style/Theme.TopMovies_NoActionBar"></activity>

        <activity android:name=".HomeScreen_Activity"></activity>

    </application>

</manifest>